import { Sidebar } from "@/sections";
import { calculateEndDate } from "@/utils";
import moment from "moment";

export default function Home() {
  
  return (
    <main className="grid mf:grid-cols-12 grid-cols-1">
     
    </main>
  );
}